import React from 'react';

export const Header: React.FC = () => {
  return (
    <header style={{ padding: '20px', textAlign: 'center', background: '#111', color: '#fff' }}>
      <h1>Gabarita-AI</h1>
    </header>
  );
};

export default Header;

